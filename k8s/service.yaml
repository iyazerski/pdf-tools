apiVersion: v1
kind: Service
metadata:
  name: pdf-tools
  labels:
    app: pdf-tools
spec:
  type: ClusterIP
  selector:
    app: pdf-tools
  ports:
    - name: http
      port: 8091
      targetPort: http
